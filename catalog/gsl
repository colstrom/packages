
# -*- shell-script -*-

package=(
    name=gsl
    version=2.4
    origin=https://ftp.gnu.org/gnu/gsl
    files=(
        gsl-${_.version}.tar.gz
    )
)

configure=(
    options=(
        --enable-shared
        --enable-static
        --with-pic
    )
)
