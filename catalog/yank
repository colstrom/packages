
# -*- shell-script -*-

package=(
    name=yank
    version=0.8.3
    origin=https://github.com/mptre/yank/archive
    files=(
        v${_.version}.tar.gz
    )
    requires=(
        make_arguments=1
    )
)

make=(
    arguments=(
        "CC=${CC}"
    )
)
