
# -*- shell-script -*-

package=(
    name=libssh2
    version=1.8.0
    origin=https://www.libssh2.org/download
    files=(
        libssh2-${_.version}.tar.gz
    )
)

configure=(
    arguments=(
        "ACCEPT_INFERIOR_RM_PROGRAM=yes"
    )
)
