
# -*- shell-script -*-

package=(
    name=zeromq
    version=4.2.2
    origin=https://github.com/zeromq/libzmq/releases/download/v${_.version}
    files=(
        zeromq-${_.version}.tar.gz
    )
)

configure=(
    arguments=(
        "ACCEPT_INFERIOR_RM_PROGRAM=yes"
    )
)
